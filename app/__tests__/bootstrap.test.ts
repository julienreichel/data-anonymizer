import { describe, expect, it } from 'vitest';

/**
 * Placeholder smoke test — confirms Vitest can discover and run tests in `app/`.
 * Replace with real unit tests as frontend components are implemented in later EPICs.
 */
describe('app bootstrap', () => {
  it('test framework is operational', () => {
    expect(true).toBe(true);
  });
});
